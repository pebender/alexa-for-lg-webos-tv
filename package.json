{
  "name": "alexa-for-lg-webos-tv",
  "version": "0.0.1",
  "description": "An Alexa skill and a bridge for adding Alexa support to LG WebOS TVs that don't have native Alexa Support",
  "author": "Paul E. Bender <pebender@gmail.com> (http://www.backinthirty.net)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pebender/alexa-for-lg-webos-tv/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pebender/alexa-for-lg-webos-tv.git"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@microsoft/tsdoc": "0.16.0",
    "@tsconfig/node24": "24.0.5",
    "copyfiles": "2.4.1",
    "eslint": "10.8.1",
    "eslint-import-resolver-typescript": "4.4.5",
    "eslint-plugin-tsdoc": "0.5.2",
    "globals": "17.11.0",
    "markdownlint": "0.41.1",
    "markdownlint-cli2": "0.23.2",
    "npm-pack-zip": "1.3.0",
    "typedoc": "0.28.20",
    "typedoc-plugin-markdown": "4.12.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0"
  },
  "scripts": {
    "clean": "rm -rf node_modules package-lock.json && (cd packages/common && npm run clean) && (cd packages/skill && npm run clean) && (cd packages/bridge && npm run clean)",
    "install_modules": "npm install && (cd packages/common && npm run install_modules) && (cd packages/skill && npm run install_modules) && (cd packages/bridge && npm run install_modules)",
    "lint": "npx eslint `find packages/*/src/ | grep '\\.ts$'` && npx markdownlint-cli2 'README.md' 'doc/**/*.md'",
    "build": "(cd packages/common && npm run build) && (cd packages/skill && npm run build) && (cd packages/bridge && npm run build)",
    "build_doc": "rm -rf doc_generated && mkdir doc_generated && npx typedoc",
    "pack": "(cd packages/skill && npm run pack) && (cd packages/bridge && npm run pack)",
    "all": "npm run clean && npm run install_modules && npm run lint && npm run build && npm run build_doc && npm run pack"
  }
}